
export default defineAppConfig({
  // Navbar state that can be changed from any page or component.
  // We use this to make the navbar transparent on the dashboard hero.
  navbar: {
    isTransparent: false
  }
})